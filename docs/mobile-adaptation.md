> ⛔ **本层已于 2026-09-25 按用户要求整体移除。** 本文档保留为**历史记录**：
> 它记录了为什么会有这一层、踩过哪些坑、以及 dsh 0.1.7-rc.2 是如何把它拆掉的
> （见文末「0.1.7-rc.2 会拆掉注入的插件」一节 —— 那是移除的直接原因）。
> 现在 App 只做三件事：全屏 WebView + SSH 隧道 + 原生终端，页面完全交给 dsh 自己的响应式布局。

# 手机端适配层（自研）

> 2026-09-13 起，这一层是**本仓库自己的代码**。此前它是 vendored 的第三方
> [dsh-web-mobile](https://github.com/mexiaosqwq/dsh-web-mobile)（MIT，292 KB / 26 模块），
> 我们靠「在上游文件体内重打补丁」维持它 —— 每次上游发版都要照流程重打一遍，补丁与上游
> 代码混在一起，说不清哪一行是我们的。上游插件与其许可证已删除。

## 它是什么

一个标准的 **dsh 客户端插件**（浏览器半边），只做手机端的界面适配，不改任何服务端行为：

```
android/app/src/main/assets/plugins/
├── dsh-handheld-mobile.js    ← 适配层本体（我们自己写，约 15 KB）
└── mobile-bootstrap.js       ← 注入引导（doc-start 钩 window.__DSH_BOOT__，补一条 entry + batch）
```

加载链路（**服务端零改动**）：

```
App 启动 → WebView 加载 dsh 页面
  └─ addDocumentStartJavaScript(mobile-bootstrap.js)  ← 读 assets，占位符换成 App 常量
       └─ 启动图里多一条 entry/batch：id=dsh-handheld-mobile, url=/plugins/??…/client.js&rev=…
            └─ WebView 请求那个 URL
                 └─ shouldInterceptRequest 命中 → 返回 APK 里的 dsh-handheld-mobile.js
```

插件的运行时外部依赖只有 `react` / `react/jsx-runtime` /
`@deepseek-ai/dsh-client-ui-primitives`，都在前端壳的 staticModules 种子里。

## 它依赖宿主什么

契约文件 `scripts/mobile-hooks-contract.json` 是唯一事实来源，由
`scripts/check-mobile-hooks.mjs` 维护与校验：

| 依赖 | 用途 |
|---|---|
| `data-phase` | 判断会话处于哪个阶段（`hero` / `active` / `settling` / `inert`）：hero 阶段没有会话头，浮动入口在那时出现 |
| `data-sidebar-collapsed` | **抽屉的开合信号**：宿主窄屏下的 `!narrowExpanded`，我们不自己维护开合状态 |
| `data-sidebar-right-panel` | 右侧边栏面板（取值 `fullscreen` / `push`）：手机上它只要打开就必然是 fullscreen |
| `data-sidebar-right-mode` | 右侧边栏那个「全屏 / 退出全屏」按钮：手机上它与「收起」是同一个动作，隐藏（见下） |
| `data-composer-stats` | 输入框上方那行统计（轮/步 · tok/s、tokens · 缓存命中）：窄屏下重排它，把手机宽度用满（见「设计取舍」） |

除这些属性，还依赖一组结构（类名是 CSS Modules 的哈希前缀，只能用 `[class*=…]` 匹配）。
**它们同样进了契约，而且是两份清单**（2026-09-14 补，见 `known-issues.md` §六 B1）：

| 清单 | 含义 | 找不到时 |
|---|---|---|
| `classHooks`（17 个） | dsh **核心客户端包**里的界面（`_frame` / `_sidebarCol` / `_turnStatus` / `_navList` / `_sep` …） | 完整检查**判失败** —— 那条适配规则已经空转 |
| `classHooksPlugin`（1 个） | **可选插件包**提供的界面（`_moreButton` 来自 `dsh-session-log-export`） | 只提示，不判失败：没装那个插件时规则本来就是空转 |

局限（写下来免得当成没做）：短子串（`_split` / `_count` / `_menu`）在 dsh 里命中多个模块，
所以这个金丝雀只能发现「后缀整体消失」，发现不了「我们想指的那个元素换了模块」。
更强的一条得按真实 DOM 结构断言，需要浏览器 + 一个跑着的 dsh 实例。

结构如下：

```
div[class*="_frame"]                     外壳网格：侧栏 | 中栏 | 右栏（还有 _overlayLayer）
  > div[class*="_sidebarCol"]            侧栏 —— 被我们画成抽屉
div[class*="_root"][data-phase]          会话
  > header > div[class*="_titleRow"]
      > div[class*="_crumbs"]            标题道（可压缩）
      > div[class*="_headerActions"]     动作道（不让步）
      > [class*="_root"]:has(> button[class*="_trigger"]) > [class*="_menu"]   头部弹层
```

dsh 哪天改了这些，CI 的 `Mobile adaptation contract` 会红，而不是手机上一声不响地坏掉。
**注意扫描范围是整份 bundle**：适配规则大量写在 CSS 里，只扫 JS 的 `querySelector` 会让
依赖漏出契约（2026-09-13 改为扫全文，钩子集合因此从 6 个收敛到 2 个）。

## 设计取舍（写下来免得以后当成 bug 查）

- **抽屉的开合状态归宿主**。我们只调用 `ctx.layout.toggleSidebar()`，读它写的
  `data-sidebar-collapsed`。自己再存一份状态迟早会和宿主的窄屏逻辑打架。
- **抽屉用 `left` 定位，不用 `transform`**：设置对话框（`position: fixed` 浮层）渲染在
  **侧栏里面**（`SettingsRoot` 注册进 `sidebar.settings` 槽）。`transform` 或
  `will-change: transform` 会让侧栏成为 fixed 后代的包含块 —— 对话框就被缩进抽屉的
  坐标系（真机实测：只有 329px 宽、贴着屏幕左边，视口是 384px）。用 `left` 位移没有
  这个副作用。
- **CSS 优先，JS 只做三件事**：打 `data-handheld` 标记、注册两个槽（会话头的目录按钮、
  外壳浮层的遮罩与浮动入口）、在抽屉里点会话行时收起抽屉。
- **槽而不是手塞 DOM**：遮罩与按钮注册进宿主的 `shell.overlay` /
  `conversation.session.header.actions` 槽，跟着 React 的重渲染走 —— 手塞的节点会在
  会话切换、面板重挂时丢。
- **计数收进 aria-label**：窄屏下后台任务胶囊只留状态点与下箭头，完整计数仍在按钮的
  `aria-label` 上（状态与无障碍信息都没丢，只是不再霸占标题宽度）。
- **浮层住在抽屉里，但只借住 DOM**：设置对话框由宿主注册进 `sidebar.settings` 槽，
  所以它在 DOM 上是侧栏的后代；但它 `position: fixed`、盖满整屏，视觉上是视口级的。
  这条「借住」关系有两个坑，1.0.11 / 1.0.12 都堵上了：① 抽屉收起时的 `pointer-events: none`
  会把对话框一起冻住（它是继承属性）→ 抽屉里只要有 `[role=dialog][aria-modal]`，
  整列就把指针要回来；② 「抽屉里点一下就收起来」的启发式会把「关对话框」的那一笔
  （对话框里的文字、以及**作为面板兄弟的遮罩**）当成"选完了" → 抽屉里有模态时整条
  启发式让位，判据按祖先范围而不是 `target.closest`。
  详见 `docs/known-issues.md` §五。
- **BACK 三级阶梯**（`MainActivity.handleWebBack()`）：手机上「关掉这一屏」的直觉就是系统
  返回键，所以 BACK 按 **模态 → 抽屉 → 原语义** 依次尝试：① 页面里有模态（设置页）时派发
  一次 Escape（dsh 的模态在 document 上监听 Escape），240ms 后复查，模态还在就往下走；
  ② 抽屉开着就点我们自己的遮罩收起它（开合的真相始终在页面侧）；③ 都没有才走原来的语义
  （网页历史 → 连接屏 → 退到后台）。每一级都要求「真的做到了」才停，绝不让 BACK 变成空操作。
- **右侧边栏在手机上只有一个出口**（1.0.14）。宿主自己算的是
  `autoFullscreen = viewportWidth < 768`、`fullscreen = autoFullscreen || mode === "fullscreen"`，
  所以 384px 的视口里右侧边栏**只要打开就是全屏**；而面板右上那个「全屏 / 退出全屏」按钮的
  onClick 是 `if (fullscreen && autoFullscreen) setExpanded(false)` —— 手机上它和
  「收起右侧边栏」**完全是同一个动作**（真机复现：点它，面板直接关掉），唯一多出来的效果是
  把持久化的 mode 写成 `push`，会在用户回到电脑上打开同一个 dsh 时改变面板的初始形态。
  按「做不到 / 重复的入口不留」把那个按钮隐藏（`data-sidebar-right-panel=fullscreen` 时），
  判据用宿主写的两个 `data-*` 而不是 aria-label 的语种。
- **做不到的入口不留**：凡是「动作发生在电脑上」的入口，手机上按了都没反应，一律隐藏 ——
  ① 工作区标题行的 `+`（目录选择器判成 native，对话框开在电脑桌面）；
  ② 会话头右上角的「在 文件管理器 中打开工作目录」（`open-in-app`：宿主探测本机应用，
  在本机打开工作目录）；
  ③ 会话头那枚「⋯」（`session-log-export`：唯一功能是下载 Session 日志）。
  本项目不动服务端 composition，所以不去改宿主的判定，直接隐藏入口。
- **统计行在窄屏用满宽度**（`data-composer-stats`，1.0.18）。宿主给这一行的是
  `width:100% + max-width:--dsh-chat-content-width + 左右各 32px 留白 + justify-content:center`；
  而它住在 composer dock 那个槽里（`conversation.composer.dock` → `.uV2eYG_root`，左右再各 16px），
  `--dsh-chat-content-width` 在窄屏又恒取下限 680px（所以 `max-width` 形同不存在）。
  算下来 384px 的手机上只有 **288px** 可用，而两个胶囊（`9 轮 298 步 · 111 tok/s` /
  `62.6M tok · 缓存命中 98%`）在 13px 下要 ≈349px —— **两个都被省略号吃掉一截**，两侧却空着
  32px（真机截图：`111···` / `缓存命···`；用户的原话是「没有利用完手机屏幕宽度」）。
  改法：左右各 12px、两个胶囊分列两端（`space-between` 把余量吃掉，而不是堆在中间）、
  字号 **11.5px**、分隔符左右 2px、装不下时 **`flex-wrap` 换行**（宁可两行，也不用省略号）。
  **只在 ≤560px 生效**：448px 起宿主那套居中布局本来就装得下，不能让横屏与小平板也贴到屏幕两边。

  这一条的真机教训值得单记：**第一版只按视口宽度建模**（以为有 360px 可用），小回环全绿、
  装上真机照旧 `111···` —— 漏了父容器那 32px，而 12px 字号在小回环里的余量只有 6px，
  真机字体（Roboto / Noto Sans CJK 比 fixture 的字体宽一点）一宽就吃光。定位靠真机截图反推：
  行最左的 ink 起点 37.1 CSS px = 16（父）+ 12（自己）+ 8（胶囊内边距）+ 图标内缩。
  小回环现在按父容器建模，并把**余量 ≥ 12px** 写进断言 —— 「装是装下了」不算过，
  要留得出真机字体的余量（`scripts/composer-stats-lab.mjs`）。

  **真机验证（2026-09-17，SM-S9280 / Android 16 / release APK，384 CSS px）**：同一台手机、
  同一份会话，1.0.17（第一版）显示 `11 轮 398 步 · 107 to···` / `89M tok · 缓存命中 9···`；
  1.0.18 显示 `11 轮 456 步 · 110 tok/s` / `110M tok · 缓存命中 98%` —— **两段完整、单行**。
  截图量出来的几何：ink 跨度 37.1 → 337.1 CSS px（行的内容盒是 28 → 340 = dock 16 + 自己 12），
  两个胶囊之间空 22.7px，余量 ≈18px（与小回环的 22px 之差就是字体宽度差）。

## 本版**没有**做的（与上游能力的差距，按需再补）

| 上游有 | 我们 | 说明 |
|---|---|---|
| 抽屉边缘滑动手势（上游 62 KB） | ❌ 没做 | 只能点按钮开合；手势要处理与可拖动浮动组件互让、和系统返回手势冲突，等有明确需求再做 |
| 输入区/键盘的细节防护 | ❌ 没做 | 实测手机上输入区本来就正常 |
| 设置弹窗的手机布局 | ✅ 做了 | 桌面是 800px 面板里 188px 竖导航 + 内容列，手机上内容列被压到 ~100px（一个字一行）。改成近全屏 + 导航变顶部 **2×2 网格**（第 8 节；为什么不是横滑 tab：360px 下第 4 个会被切掉）。四周留 16px（60 设备像素 ≈ 4.2mm）当「点外面关掉」的遮罩条 —— 原来 8px ≈ 2.1mm，手指点不到 |
| 预览浮层的全屏化 | ❌ 没做 | 实测预览在手机上已可用（全屏态自带「退出全屏」） |
| 第三方 UI 套件（dsh-web-ui / aionui）兼容层 | ❌ 不做 | 本部署没装那套东西 |
| 会话行的长按菜单 | ❌ 没做 | 宿主自己的 `⋯` 菜单够用 |
| 会话日志下载入口 | ❌ 去掉 | 宿主那枚「⋯」按钮就是它，按「做不到/不需要的入口不留」整枚隐藏（见上一条）；`/export` 命令仍在 |

## 怎么验证

三层，和以前一样，只是第一层的对象换成了我们自己的代码：

1. **契约金丝雀**（秒级，CI）：`node scripts/check-mobile-hooks.mjs --contract`
2. **对已安装 dsh 的完整检查**（本机）：`node scripts/check-mobile-hooks.mjs`
   —— 断言这些钩子（含类名后缀）确实还在 dsh 前端产物里
3. **统计行的 A/B 断言**（本机，需要 dsh + chromium）：

   ```sh
   node scripts/plugin-css.mjs /tmp/plugin.css
   node scripts/composer-stats-lab.mjs --css /tmp/plugin.css
   ```

   宿主那几段 CSS 从**安装产物**里现抽（不把 dsh 的样式抄进仓库），fixture 用真实 class 名，
   连**父容器**（composer dock）一起搭；360 / 384 / 412px 三个宽度各跑两遍：
   **A 不注入必须复现截断**（复现不了 = fixture 失真，B 的通过不算证据），
   **B 注入后不得截断、两侧不得留白、且不换行时余量 ≥ 12px**（真机字体更宽）。
   拿 1.0.16 的 CSS 跑会红 —— 所以它不是一条永远通过的断言。
4. **真机**：装 CI 产物 → `adb exec-out screencap`（不需要 debuggable）+ `uiautomator dump`
   取无障碍树核对元素与坐标；**命中测试**（点了有没有反应）只能用 `input tap` 驱动 + 看
   实际状态变化 —— 无障碍树给不出「这一笔被谁吃住」，而 1.0.11 修的正是这一类故障
5. **真机帧性能**（固定协议，见下面「性能」一节的教训）：

   ```sh
   node scripts/device-perf-verify.mjs --serial <host:port>              # 30s 滚动 × 3 轮
   node scripts/device-perf-verify.mjs --serial <host:port> --scenario idle
   ```

   它自己做三件事：**先把状态摆正**（息屏 / App 不在前台直接拒测 —— 设备 2 曾在 Dozing
   下被采过一轮，数字全是废的）、**报中位数与极差**、**并把这组数字能不能用于归因印出来**
   （极差过大时明说「不能归因」，`--strict` 下以非 0 退出）。顺带把插件自报的 `页面耗时`
   汇总成表 —— 单点开销看这条，比看整页的帧统计准。

另有一个**本地渲染回环**用于快速迭代 CSS（这个沙箱里 Chromium 发不出 HTTP，所以用
fixture 页面 + 真实的 dsh 组件 CSS，`file://` 加载）：见 `docs/mobile-ui-verification.md`。

## 它与 App 的原生通道（1.0.15 起）

适配层多做了一件事：**把「这一轮生成结束了」告诉 App** —— 任务完成通知的触发源。

| | |
|---|---|
| 判据（完成） | dsh 的「深度求索中…」指示器：`dsh-client-ui-chat` 的 `div[class*="_turnStatus"][role=status]`。它随 `running` 挂载/卸载，我们盯**从有到无**的那次跃迁。**必须是可见的那一个**（见下） |
| 判据（等你在手机上点一下） | `[data-question-key]` / `[data-approval-key]` / `[data-plan-review-key]` —— 提问、工具审批、计划确认三张卡，pending 时挂载、回答后卸载。用 key 去重（同一张卡重渲染不会重复提醒） |
| 通道 | `window.dshNative.postMessage(...)` —— WebView 的 `addWebMessageListener`（`androidx.webkit`），origin 只放行隧道实际会用的两个（`SshTunnel.PORT_CANDIDATES`），**只进不出**（App 不向页面发指令） |
| 消息 | `{"type":"turn-start"}`、`{"type":"turn-done","title":…,"ms":…}`、`{"type":"needs-input","title":…,"key":…}`、诊断心跳 `{"type":"turn-state","nodes":N}` |
| 没有桥时 | 静默跳过 —— 同一份 bundle 在桌面浏览器里只是不通知，不影响适配 |

### 2026-09-17：「完成后没有收到弹窗提醒」——判据必须取**可见**的那个节点（1.0.19）

真机日志（`adb logcat -s DshApp`，0.1.11 + 插件 1.0.18，开关=true、权限已给、
`dsh-turn` 渠道存在）：

```
00:22:33  页面报告：一轮生成开始（pageBusy=true）
00:28:41  页面报告：一轮生成结束（367948ms，…，前台=true）→ App 在前台，不发通知
00:29:57  页面报告：一轮生成开始（pageBusy=true）
（此后一整小时：一条「页面报告」都没有 —— 那一轮早已结束，用户也回来看过了）
```

也就是说：`turn-start` 收到过两次、`turn-done` 只在第一次收到，之后**观察者卡死**。
原因是判据只按 `found.isConnected` 判、用 `document.querySelector` 取**第一个**
`_turnStatus`：对话与轨迹两个面板各挂一份同名节点，隐藏的那份 `isConnected` 永远为真 →
`running` 一直停在 true → 此后再也不报结束。

改法：`pick()` / `live()` 都要求 **`isConnected && getClientRects().length > 0`**
（后者 = 真的被布局出来，`display:none` 与零尺寸都为 0），两个面板谁可见认谁。

同时还加了**诊断心跳**：跑一轮期间每 60s 发一条 `{"type":"turn-state","nodes":N}`，
App 把它记进日志（`DshApp.onPageMessage` 的兜底分支现在会打印消息内容）。
这一层的失败模式是「一声不响地不再报」—— 没有心跳，事后只能从「一条都没有」反推，
连「观察者死了」还是「它看错了节点」都分不出来。

三条不显然的实现约束（都写在代码注释里）：

1. **不能用 `requestAnimationFrame` 节流**（上面那个标记兜底 effect 用了没事，标记丢了只是不好看）：
   页面在后台时 rAF 不跑，而我们要捕获的恰恰是「用户在别的 App 里」时发生的那次结束。
   改成「记住上次找到的节点 + `isConnected`（O(1)）判断它还在不在」。
2. **不能用定时器判「输出停了」**：后台的 `setTimeout`/`setInterval` 会被节流到分钟级，
   而 `MutationObserver` 回调是微任务，跟着 JS 任务走，不受节流影响。
3. **`webView.pauseTimers()` 与这个功能直接冲突**：它是全局的（"layout, parsing, and
   JavaScript timers"），而「结束了」这个信号要靠页面重新渲染出来。所以
   `MainActivity.onPause()` 只在页面空闲时才暂停定时器（判据是 `DshApp.pageBusy`，
   由 `turn-start`/`turn-done` 维护）—— 代价是生成期间后台多耗一点电。

## 空间：留白到底是谁的（1.0.25 – 1.0.42）

用户 2026-09-19 连报四条「留白太多 / 两侧不对称 / 顶部大片留白」。真机量下来是**四个来源**，
逐条证据见 `docs/release-notes-0.1.14.md`；这里只留结论与下次要用的判据：

| 来源 | 实测 | 修法 |
|---|---|---|
| 宿主正文内边距 | `.EvIC1a_scroll{padding:16px calc(…+16px)}` → 两侧各 32px | ≤560px 覆盖成 12px（**+40px 内容宽度**） |
| 会话头高度 | `min-height:76px` **压不动** —— 76 是内容算出来的（10+30+10+25） | 动内容：`padding-top 10→4`、标题行 `min-height 30→24`、页签 `margin-top 10→4` |
| 挖孔安全区 | viewport 没 `viewport-fit=cover` → Chromium 内缩 34px；补上之后宿主外壳又用 JS 内联塞了 `padding-top:35px`（挖孔本体只有 ~11px） | `mobile-bootstrap.js` 补 cover；再用 `html[data-dsh-cover] [class*="_frame"]:has([data-phase]){padding-top:12px !important}` 压内联 |
| **右侧滚动条槽** | `.wSkVaW_scrollBody{scrollbar-gutter:stable;margin-right:2px}` + `--dsh-scrollbar-width:8px` → 右侧恒定多 10px（每块内容：输入卡 27.5/37.3、统计行 41.9/51.5） | `[class*="_scrollBody"]{margin-right:0;padding-right:4px}` → 右侧 8+4 = 12 = 左侧（**滚动条保留**，内容 +10px） |

判据速查：**Δ 恒定 = 某处固定让位**（内边距写错只会差一处）；**Δ 只在个别元素上 = 那个元素自己的盒子**；
**上下要对齐 = 两处必须共用同一个值**（目录按钮是绝对定位，`top` 写死就会在压缩 header 时掉队 ——
真机上差 10.1px，用户一眼看到「两边上下不对称」）；**宿主把按钮藏在 hover 里 = 手机上永远没有**（工作区行的 `⋯`/`+`、会话行的 `⋯` 都只有
`:hover`/`menuOpen` 才 `display:inline-flex` —— 窄屏必须一律显示；全屏抽屉里还顺手撑开了宿主根节点的内联 width（265 → 满宽）并把行高/字号/图标抬到手指档（44px / 15px / 20px）✓）。

**三条教训**：

1. **header 的 `padding-right` 不要碰**：宿主 `.wSkVaW_headerCorner` 带 `margin-right:-16px`，
   把它从 28px 压到 12px 会让右端按钮落到 −4px（用户报「右侧边栏按钮太靠右了」）。两侧的空间
   优化只作用于正文与统计行。
2. **量，不要估**：小回环（`composer-stats-lab.mjs`）对真机字体乐观 —— 真机余量 ≈ 小回环余量 **+32px**；
   截图量化只能数深色像素（左/右/上第一个深色像素 ÷ dpr）。所以这一版加了四个**一次性**诊断
   （`viewport-diag` / `stats-diag` / `layout-diag`（祖先链 + `env()` 探针）/ `side-diag`（滚动体的
   margin/padding/gutter + `offsetWidth − clientWidth` 的真实滚动条宽度）），判据从「我估」变成
   「页面自己报的数」。
3. **盒模型问题先在小回环里复现再改**（负对照不复现就不认结论）：统计行与这次的滚动条槽都是
   小回环先复现、真机再确认。滚动条那条的坑是 `--hide-scrollbars` —— 带上它滚动条宽度归零，
   负对照就永远复现不出来。

## Web UI 逻辑：两个「只能整页重载」的坑（1.0.43，2026-09-20 只读研究）

一次针对宿主的只读研究（三个方向：侧栏数据链路 / 网关重连 / 触摸可用性；证据全部带
安装产物的路径与行号）找到两个**逻辑层**的坑，它们的表现就是用户报过的
「列表要等几分钟」和「隧道断一次就必须整页重载」：

### ① 会话列表的基线只拉一次，失败不重试、挂住就永久卡住

- 侧栏的「工作区」头走 `workspace.follow` 流（插件 `apply()` 阶段就开流，不等握手），
  而**会话行**走一次性 unary `session/list`，触发点只有 `connection/reset`；
- `SessionManager.refreshList()` 是单飞：`if (this.listInflight !== null) return this.listInflight`
  —— 那次 POST 若**挂住**（隧道半开、无 caller signal、无 timeout），之后所有调用都复用同一个
  卡死的 promise，连重连触发的 reset 都救不回来；
- 失败只把 `listState` 记成 `"error"`，`phase` 停在 `"pending"`，而 `sessions.list` 投影里
  **既没有 state 也没有 error**，UI 连 loading/错误都不显示 → 「工作区 › xsj」下面一片空白；
- 公开入口 `ctx.sessions.refresh()` **存在但全仓没有任何 UI 调用它**。

**我们的做法（不改宿主）**：插件在页面里做复健 —— 首屏后 6s/15s/35s、抽屉打开时、
回到前台时体检「侧栏有没有会话行」，为空就调用 `ctx.sessions.refresh()`（节流 5s、上限 8 次）。
抽屉打开这一条最实用：用户下一步就要看列表，空就当场补一次。

### ② 隧道断过一次后，页面不会自己恢复

- 客户端**有**指数退避重连（500ms×2 封顶 10s，就绪超时 15s），但没有任何**活性探测**：
  隧道断而 close 到不了页面时，socket 仍是 `OPEN`、请求永远挂住；
- 历史流的重试预算只有 1 次：`waitForRemoteStreamRetry` 里「当前 generation 仍存在 → 第 2 次直接终态」
  → 包成 `RemoteError('gateway/internal', 'api gateway: Remote stream WebSocket closed')`，
  正是页面那行「历史加载失败」；
- 终态后**没有任何自动重开路径**：`connection/reset` 只刷列表与子代理，不碰
  `openState==='error'` 的会话窗口（`followCurrent()` 还被 `current === this.watched` 去重）；
- 全仓**没有** `visibilitychange` 处理，`online/offline` 对 SSH 隧道也不敏感。

**我们的做法（不改宿主）**：回到前台且侧栏为空 → 调用 `ctx.connection.reconnect()`
（等价于设置里那颗「立即重连」，已有 UI 入口，只是没人替用户点）。每次动作都往 App 日志
写一条 `界面复健：{...}`，下次真机上复现时能直接看到它到底跑没跑、跑了几次。

### 还没做、留给用户决策的两件事

1. **宿主心跳宽限**：服务端每 2s ping、连续 2 次没 pong 就 `terminate`（`MAX_MISSED_HEARTBEATS = 2`
   硬编码），实际宽限 ≈6s。可以在**用户 profile** 的 `cordis.patch.yml` 里覆盖
   `typert-gateway.config.websocketHeartbeatIntervalMs: 10000`（宽限 → ≈30s，零代码）。这会同时
   影响桌面 GUI（多占最后一条死连接几十秒），所以没擅自改。
2. **触摸可用性的整批盘点**（研究的第三份报告）：最狠的三处是 ①侧栏行的 hover-only 操作与
   HoverCard（会话预览、复制 cwd 在手机上完全不可达）②HTML5 拖拽的工作区排序/移动
   （手机上没有任何替代路径）③`opacity:0` 的 hover 揭示按钮（**隐形热区 + 隐形入口**）。
   第 ③ 类已在 1.0.43 里统一显形（窄屏 `opacity:1 + pointer-events:auto`，含宽 markdown 表格
   的横滑门控）；①②属于功能级补做，需要单独立项。

## 手指档命中区（1.0.45 – 1.0.48，2026-09-20 只读盘点 + 真机体检）

**盘点**（46 个 client 包的 CSSModule 全扫）：primitives 的 `Button` 默认高 **36px**、`size=sm` **28px**、
`DisclosureRow` 行 **24px**（几乎每个包都用）；一大批 **28×28** 图标按钮（侧栏、会话头操作、工具/技能卡、
Goal 条、右栏、附件删除 18×18、dockkit 标签关闭 20×20）；三个极端小的：**工作区行内操作 16×16、
轮次刻度 20×10、侧栏拖拽把手 8px 宽**。比「小」更致命的是 hover 门控（看不见 = 点不到）—— 那一类已在
1.0.37/1.0.38/1.0.43 处理。

**改法**（只在本插件、只在窄屏）：默认按钮 `[class*="_button_"]` 与整行可点的 `[class*="_row"][role="button"]`
抬到 **44px**；小图标按钮只**扩命中区**（`::after` inset ±8px，视觉不变），页签因为左右相邻只往上下扩；
抽屉里我们注入的行内操作直接 32×32。

**真机体检**（插件里一次性 `tap-diag`，写进 App 日志）——它量的是「可点击元素总数 / 视觉 <44 的 / 真正
点不到的 / 隐形但仍吃点击的（opacity:0 或 visibility:hidden 且 pointer-events 不是 none）」：

| 版本 | 可点击 | 视觉偏小 | 小且没扩过 | **隐形热区** |
|---|---|---|---|---|
| 1.0.46 | 167 | 67 | 46 | **3** |
| 1.0.47 | 168 | 69 | 48 | 3 |
| **1.0.48** | 165 | 67 | 47 | **0** ✓ |

隐形热区归零是这一轮最干净的收益：它们分别是 dockkit 的 `_addTab` / `_iconButton` 与
`P3OORG_iconButton`，用的是 **`visibility: hidden`** —— 我第一版只写了 `opacity: 1` 所以无效 ✗
（1.0.48 补 `visibility: visible`，日志当场验证）。

**仍未解决、需要产品选择**：「视觉偏小」还有 67 个（消息操作 27×27、各种 27×28 触发块、页签 25×25…）。
现在走的是「视觉不变、扩命中区」，但 `::after` 会被 `overflow: hidden` 裁掉、相邻按钮的命中区也会互相盖住，
所以**不能保证每个都真的变好点**（体检里那个「小且没扩过」的数是靠 `elementFromPoint` 四向探针算的，
受相邻元素影响，只能当参考）。另一条路是**直接把这些控件放大到 ≥40px**（更像原生 App，但界面密度会明显下降）。

## 性能：适配层的常驻开销在哪（1.0.64 – 1.0.69，2026-09-22/24）

起因是「现在 web 性能会不会有问题」。先把**常驻**的活儿数清楚（`node --check` 之外全是数出来的，
不是估的）：

| 类别 | 常驻数量 | 说明 |
|---|---|---|
| `MutationObserver` | 7（其中 **5 个** `subtree: true` 挂在 `documentElement`/`body` 上） | 帧标记、侧栏变宽、轮次监视、待输入监视、抽屉开合、右栏探针、面板遮挡 |
| `setInterval` | 真正常驻 **2** 个：面板遮挡 `sync`（1s）、右栏探针 `look`（2s） | 另 1 个是轮次心跳 **300s**（可忽略）；行尾菜单 30ms 轮询只在点按后存活 ≤600ms；抽屉观察者挂载轮询 1s 且 30s 内自清 |
| 几何读/命中测试 | `getBoundingClientRect` **19** 次、`getClientRects` **3** 次、`elementsFromPoint` 3 次、`elementFromPoint` 2 次 | **真正的开销在这里**：每个都会强制一次同步布局 |
| 一次性诊断 | `layout-diag` / `stats-diag` / `side-diag` / `tap-diag` / `right-probe` / `ui recovery` | 全是加载后固定延时的 `setTimeout` 或点按驱动，**不周期跑**（这一点原先没有确认过，是这次核的） |

**结论：成本不来自插件体积（2170 行、17 段 CSS），而来自「在脏树上读几何」。** 读 `getBoundingClientRect()`
时若 DOM 刚被改过，浏览器必须先做一次布局（forced synchronous layout）；真机上这个页面在流式输出期间
DOM 几乎永远是脏的，于是**回调里每一次几何读都是一次 4~11ms 的布局**（插件自报的 `panel-sync` 耗时）。

### 改了什么

| rev | 改动 | 依据 |
|---|---|---|
| 1.0.64 | `--dsh-handheld-vh` 只在**高度真的变了**时才写（原先每个 `visualViewport` 的 resize/scroll 都写 → 根元素样式在滚动中反复失效） | 代码里唯一一处「随滚动反复付钱」的地方 |
| 1.0.65 | 面板遮挡的 12 点采样**最多 2 秒一次**（面板刚开/刚合强制一次） | 每次采样 12 次 `elementsFromPoint`，实测 4~11ms；原先最多 5 次/秒 |
| 1.0.66 | 9 条**文档级** `:has()` 换成 `html.dsh-handheld-mobile` 类；侧栏行内操作的重扫降到 ≤2 次/秒（带尾随一次） | 文档级 `:has()` 是样式重算里最贵的形式；行内操作原先每次变更做 ~2N 次查询 |
| 1.0.67 | 面板的「没展开」判据挪到**读几何之前**：宿主把 dock 的 `expanded` 写在面板根上（`data-sidebar-right-open`，与 `aria-hidden` 互反），零布局就能判掉；面板元素做缓存 | 原顺序是「先 `getBoundingClientRect()` 再判 2 秒节流」→ 闲置 1 次/秒、流式 ~5 次/秒的强制布局**全发生在空处** |
| 1.0.68 | 只改注释：订正 1.0.67 里那句**错误的**「顺带修掉一个语义错」（见下） | 真机 `right-probe` 取证 |
| 1.0.69 | 面板遮挡改成**轮转采样**：开合瞬间扫满 3 行，稳态每 2 秒只扫 1 行（点数一个不减，只是摊到时间上） | 面板打开 60s 实测：每个 10s 窗口都有一次 ≥4ms 扫描（12 次 `elementsFromPoint`＝12 次强制布局） |

**1.0.67 的真机验收**（设备 1，装 CI 产物）：点右上角开关 → 面板打开、插件报
`面板遮挡：{"open":true,"n":4,"at":["div._tabStrip_17p4l_156", …]}`（4 个遮挡被让开）＋一条
`页面耗时：{"what":"panel-sync","ms":17.2,"n":4}`；再点面板里的「收起右侧边栏」
（无障碍树实测 `[1308,78][1421,191]`，标签就是「收起右侧边栏」）→ 面板关闭 ＋ `{"open":false,"n":0}`
（标记全部还原），全程无 JS 异常。**这道门没有把面板关不掉的毛病带回来。**

> 附带记一个真机教训：面板工具栏里 `[1143,78][1256,191]` 那颗是**「新标签页」**，不是分屏；
> 我按像素猜坐标点时误触它，把 dock 开成了两个窗格（而且是**持久化**的，收起面板再打开还在）。
> 还原办法：点其中一个窗格标签上的 × —— 空窗格会被 dockkit 合并掉，回到单窗格。
> **猜坐标不如先 dump 无障碍树**，标签和 bounds 都在里面。

几何判据没有整个删掉：属性只说**该**展开，滑入动画那 ~300ms 里几何还没落定，此时采样点会打偏到面板外
命中无关元素，所以「落定」仍旧只认几何。

> ### 一次被自己的取证推翻的结论（1.0.67 → 1.0.68）
>
> 1.0.67 的 commit 里我写了一句「**顺带修掉一个语义错**：`visibility: hidden` 的元素仍然有盒，
> 所以收起状态的 fullscreen 面板会被旧判据当成『展开』，对着用户看不见的面板扫遮挡」。
> **这条是错的，1.0.68 专门改注释把它撤回。**
>
> 取证来自插件自己的 `right-probe`（App 重启后页面加载，面板处于收起态，0.7s/2.5s/5s 三次采样一致）：
>
> ```
> "panel":"P3OORG_panel","panelAttr":"fullscreen","panelPos":"fixed","panelZ":"40",
> "panelRect":{"top":0,"left":384,"w":384,"h":832}      // CSS px，视口 384×832
> ```
>
> 收起态的 rect **不是** `left:0,w:384` —— 基础规则 `.P3OORG_panel{transform:translate(100%)}`
> 把它整个推到视口外（`left:384`），而旧判据要求 `left <= 1`，所以它**一直**判的是「没展开」；
> 日志侧同样只有 `stage:"init"` 一条，从来没有收起态的 `open:true`。我当时的推理只想到了
> 「`visibility:hidden` 仍然有盒」，忘了同一层还有 transform。
>
> **教训**：`getBoundingClientRect()` 的结论不能只从 `visibility` 推 —— transform 会一起改 rect。
> 这类「听起来很有道理」的附带结论，必须有一条真机读数兜底才算数。

### 面板打开时的开销：一路量到地板（1.0.65 → 1.0.69）

这条链是「先量化、再改、再量化」跑完的，每一步的数字都是设备 1 上 60 秒面板保持打开

| rev | 一次扫描采样点 | 60s 内 ≥4ms 的自报条数 | 单次耗时 |
|---|---|---|---|
| 1.0.64（未节流） | 12 点 / 每次 sync | 刷屏（每 10s 上限） | 4~11ms |
| 1.0.65（2 秒节流） | 12 点 | 6 条 | 5.3–7.9ms |
| **1.0.69（轮转采样）** | 开合 12 点 / 稳态 3 点 | **3 条** | **4.2–4.8ms** |

首扫行为逐字不变（`panel-blockers open:true n:4`，命中的还是
`div._tabStrip_17p4l_156` + 三个路径 span），收起仍然 `open:false n:0` 全部还原，无 JS 异常。

> **为什么只降到 3 条而不是 0：成本地板是「一次强制布局」本身。** 12 点变 3 点后耗时只从
> 5.3–7.9ms 降到 4.2–4.8ms —— 因为**第一次** `elementsFromPoint` 就要把布局刷出来（页面在流式输出，
> 树几乎永远是脏的，这一次就是 ~4ms），后面的点在干净的树上是便宜的。也就是说：**只要还调
> `elementsFromPoint`，每次扫描就至少付一次强制布局**；现在稳态 1 次/2 秒 ≈ 2ms/s ≈ 单核 0.2%。
>
> 再往下压只剩「别扫」这一条路（已记录在下面「没做」的第 1 条），收益是 0.2% → ~0.02%，
> 而代价是把自愈从「2 秒」放宽或改成全靠 DOM 变更驱动 —— 这条链动的是用户每天要点的面板按钮，
> **0.2% 不值得为它再冒一次回归风险**。到此为止。

### 真机读数与**它的可信度**

**先说旧数据为什么不能当基线。** 2026-09-22 那批（1.0.52 → 1.0.65）是 `adb shell input swipe`
加 `dumpsys gfxinfo` 手敲的，30s 只有 **约 1020 帧（≈34fps）**；而 2026-09-24 用脚本跑同样的 30s
是 **约 3600 帧（≈120fps）** —— 这台机器 `renderFrameRate` 就是 **120Hz**。帧数差 3.5 倍说明两边
**根本不是同一个工况**（当时多半没有真的滚起来），所以那张表的差值（包括「sticky 顶栏掉到 6.51%」）
**一律作废**，两个包之间也不再引用它。

**新协议**：`node scripts/device-perf-verify.mjs --scenario scroll --seconds 30 --repeats 3 --warmup 1`
（预热轮丢弃）。设备 1 = SM-S9280 / Android 16 / 1440×3120 @120Hz，会话页流式输出中，面板收起态。

| build | session 1 | session 2 | 99th（两次） | 同包 session 间漂移 |
|---|---|---|---|---|
| 1.0.67 | 0.61% | 0.58% | 11 / 10ms | 0.03pp |
| 1.0.66 | 0.36% | 0.47% | 10 / 10ms | 0.11pp |

每个数字都是 **3 轮的中位数**，每轮各自的轮间极差 ≤0.5pp（脚本的可信度检查两次都通过）。

> **结论：1.0.66 与 1.0.67 的帧统计差异不成立（既不支持变好，也不支持变坏）。**
> 两件事同时成立：① 同一份包换一个 session 自己就会漂 **0.11pp**（`missed vsync` 甚至从 2 漂到 8）；
> ② 两个包的差是 **0.18pp**，只有噪声底的 1.6 倍，而且 `n=2`。
> **1.0.67 的依据不是这张表，而是代码路径**：它删掉的是「面板关着时 1 次/秒 + 流式 ~5 次/秒的
> `querySelector` 全树遍历与 rect 读」，没有任何一条机制能让它变慢 —— 所以那 0.18pp 更可能来自
> 我控制不了的东西（页面内容在这 25 分钟里一直在长、机身温度、后台负载）。
> 顺带一个反直觉的机制值得记住：**强制布局读并不总是纯亏** —— 它会把布局提前到空闲处做，
> 删掉它有可能把同一份布局挪进渲染的关键路径。所以「删了强制布局 = 帧率一定变好」也不该当定理。

**这次协议本身学到的三件事**（都已写进脚本）：

1. **必须预热**：同一份包连跑 4 轮得到 7.72% / 0.67% / 0.64%（第一轮是离群值，因为刚装完包/刚收起面板，
   页面还在落定）→ 所以 `--warmup` 默认 1，且**默认丢弃**；
2. **同包重测才知道噪声底**：不做这一步，就会把 0.36 → 0.61 当成回归；
3. **要比两个包，得先冻住内容** —— 现在的 A/B 是在一个**持续流式增长**的会话页上做的，
   两组看到的内容长度并不相同。真正干净的 A/B 应该用一个**不再变化的会话**（历史会话、不发起新回合），
   再按 A/B/A/B 交错跑。这一条还没做。

### 看过但**没做**的（连同不做的理由）

1. **面板扫描改成「只在面板自己的 DOM 变了才扫」**（当前唯一还值得做的性能项）：把 2 秒心跳的
   固定扫描换成 ①面板子树上的观察者（宿主重渲染 → 立刻整轮扫）＋ ②一条更慢的兜底心跳。
   量到的收益上限很明确：稳态 1 次/2 秒 ≈ **2ms/s ≈ 单核 0.2%**，压下去也就到 ~0.02%。
   **没做**：这条链动的是用户每天点的面板按钮，而 0.2% 换不来再冒一次回归的风险（见上一节）。
2. **负向查询限流**：轮次监视和待输入监视在「节点不在」时，**每个 mutation 批次**都要做一次全树
   `querySelector(All)`，而「节点不在」恰恰是多数时间。改法是给**未命中**的查询加 ≥250ms 地板
   （判「结束」不受影响 —— 那条只看 `isConnected`，不查询）。**没做**：它需要真机数据证明值得，
   也得确认没有把「开始」的发现延迟拖到可感知。
3. **合并 4 个 `documentElement`/`body` 的 subtree 观察者**成一个 + rAF 批处理，各消费者挂脏标记。
   收益明确但会动到轮次监视那套来之不易的语义（心跳、`pick` 的挑法），**没做**。
4. **行内操作改成事件委托**（一个文档级 `click` + `closest('[role=treeitem]')`），可以整块删掉 2N 次扫描。
   **没做**：走的是宿主自己的菜单路径，必须真机验证过才敢换。
5. **顶栏 sticky 的帧开销**：归因不可信（见上），且「要不要只让键盘弹起时才 sticky」是产品选择。
6. **110 处 `!important`**：这是可维护性问题，不是运行时问题，不为了数字好看去动它。
7. **给诊断加开关**（`window.__dshHandheldDebug`，由 App 写入）：一次性诊断本身很便宜，收益主要是日志
   体积，不是帧率。

## 侧边栏的顶部空间：外层同值 ≠ 第一行同位置（1.0.71 – 1.0.72，2026-09-24）

用户报「左右侧边栏顶部空间比会话页面大」。1.0.63 那版把两侧栏的**外层** `padding-top`
对齐成了 12px（= 会话页的压缩量），但那只保证了「盒子同值」——**里面还叠着宿主自己的
内边距**，于是第一行内容并不在同一高度。

**判据**（用户看的就是这个）：截图里「顶部第一行可见内容」的 y，设备像素 / 3.75 = CSS px。
三个界面用同一段代码量，背景色用来确认当前是哪个界面（抽屉 27,27,28 / 其余 21,21,23）：

| 界面 | 改前 | 1.0.71（只改外层） | **1.0.72（结构定位）** | 与会话页之差 |
|---|---|---|---|---|
| 会话页（基准） | 22.1 | 22.1 | **22.1** | — |
| 左抽屉 | 39.5 | 27.5 | **21.3 ~ 22.7** | ≤0.8 |
| 右栏 | 34.9 | — | **23.5** | 1.4 |

**空间是谁的**（用插件自己的一次性 `topspace-diag` 读出来的祖先链，不是推的）：

```
左抽屉   我们的 12（1.0.71 归零）+ 宿主 .hHd-Xa_root 的 6（展开态 padding:6px 12px）
         + .hHd-Xa_logoRow 自己的 8（居中 logo 又消化掉一部分）
右栏     我们的 12（归零）+ 宿主 dockkit ._tabStrip_ 自己的 10（padding:10px 6px 0 10px）
```

**修法**：把叠上去的让开，而不是再加值 —— 两侧栏 `padding-top: 12px → 0`；抽屉再让开
宿主侧栏根那 6px。

> ### 一个被实测否掉的假设
> 1.0.71 里我按 `[class*="_sidebarCol"] > *` 写了「直接子元素就是侧栏根」——**没匹配上**：
> 去掉我们那 12px 后第一行只从 39.5 降到 27.5，**正好只等于我们那一档**，说明内层那条
> 根本没生效（侧栏根不是 `sidebarCol` 的直接子节点，中间有包裹层）。
> 所以 1.0.72 改用**结构定位**：谁**直接包着** logoRow，谁就是侧栏根 ——
> `[class*="_sidebarCol"] *:has(> [class*="_logoRow"])`，不依赖哈希类名、也不依赖中间几层。
> 实测后 `hHd-Xa_root` 的 `padding-top` 从 6px 变 0px（`topspace-diag` 读数）。

**键盘那套也补了设备 2**（1.0.60–1.0.62 改了三版，此前只在设备 1 / Android 16 上验过）：
点输入框 → `mInputShown=true`，键盘顶在 CSS 458，而**标题行仍在顶部**（设备像素 61–177 内有内容），
输入框被抬到 CSS 339–375 —— 既没被顶掉、也没被遮住 ✓。

**跨机复验**（2026-09-24，用户改用设备 2 后补做 —— 我自己写过「换机器要重测」）：

| | 会话页 | 左抽屉 | 右栏 | 面板工具栏按钮可点？ |
|---|---|---|---|---|
| 设备 1 SM-S9280 / Android 16 / 1440×3120 @3.75 | 22.1 | 22.7（+0.6） | 10.1 | ✓ 点「收起」→ `open:false` |
| 设备 2 SM-G7810 / Android 13 / 1080×2400 @3.0 | 21.7 | 22.3（+0.6） | 10.0 | ✓ 按钮盒 CSS y 10.0–38.0，点中即关 |

两台机器的**密度、挖孔（128 vs 88 设备像素）、Android 版本都不同**，结论一致：抽屉只比会话页
多 0.6px，面板落在宿主 tab strip 自己的 10px 上。右栏那 10.0 比会话页的**标题字形**（22）高 12px，
但与会话页**头部容器**的盒子顶（12 CSS）基本齐平 —— 三种界面的「第一行」分别是文字、药丸背景、
logo，**参考点本来就不同**，所以这里比的是容器顶而不是字形顶。

> ### 度量工具自己出过一次 bug（记下来免得再犯）
> 上面这个「顶部第一行可见内容」的判据，第一版把系统状态栏的时钟/电量按**绝对像素**屏蔽
> （为 1440 宽的设备 1 写的 `x<420` / `x>1150`）。换到 1080 宽的设备 2 后，右侧那条屏蔽
> 直接落空、左侧屏蔽盖住的范围也不一样了 —— 于是**两台机器量到的根本不是同一块内容**
> （设备 1 量到「+」号、设备 2 量到工具栏按钮），差值看着像"跨机不一致"，其实是尺子坏了。
> 改成按屏宽比例屏蔽，并且**打印触发第一行的 x 区间**（现在输出里那列 `x (316, 374)`），
> 这样"量的是谁"是自证的。教训与前面那次 rect/transform 一样：**判据本身也要有自证**。

**可用性复验**（这条历史上有坑：面板头曾落进「系统吃触摸」的带子里点不动）：
改后面板工具栏按钮的盒子在 CSS y **8.8–38.9**（原 20.8–50.9），无障碍树取中心点真机点击 →
`panel-blockers open:false n:0`，面板正常关闭 ✓。**这个位置在这台机器（SM-S9280，挖孔 128px）
上点得到** —— 老担心没有兑现，但换机器时这条要重测。

**代价**：`topspace-diag` 是**一次性**的（加载后 3s 一次 + 抽屉开合各一次），每次 5 条链 ×
4 层祖先的 `getComputedStyle` + `getBoundingClientRect`。不轮询、不进循环。留着是因为
「留白/间距」这类问题在这个项目里已经报过四次（1.0.25–1.0.42、1.0.63、1.0.71），
下次还要用同一把尺子。

## 注入链路：形状已经是最省的那种（1.0.70，2026-09-24）

起因是「现在 webview 的插入逻辑可以优化吗」。先把链路写清楚（它**服务端零改动**）：

```
document-start 脚本（addDocumentStartJavaScript，origin 收窄到隧道两个回环地址）
  └─ 钩住 window.__DSH_BOOT__ 的 setter，往启动图里补一条 entry + batch
       └─ 宿主模块加载器按 <script> 加载该 URL（atRevision 要求 URL 里必须带 [?&]rev=）
            └─ shouldInterceptRequest 命中 → 直接把 APK assets 里的 bundle 喂回去
```

**这条形状本身没有更省的做法**：不经过隧道（拦截在本进程内），没有 `evaluateJavascript`
往返，不依赖服务端配合。真正有问题的只有取字节那一步。

### 量到的四个数

| 现象 | 数字 | 处置 |
|---|---|---|
| 每次页面加载都重新喂一次 | 3 次冷启动各 1 次 | 正常（页面换了就要重喂） |
| **热路径每次请求都 `assets.open().readBytes()`** | 123,235 B / 读盘 **1~4ms** | **已修 → 缓存字节 0ms** |
| 缓存好的 `pluginBundleBytes` 只用在哪 | 只在几乎不跑的兜底 client 上 | 同上（缓存写在了错的那一边） |
| `shouldInterceptRequest` 被调用的次数 | 插件被取走那一刻已 **8 次**（整页几十次量级） | 早退前置：先子串判断，再 `Uri.parse` |

**注入时序**（插件 1.0.70 起自报，`页面耗时：{"what":"plugin-init","nav":…,"ms":…}`）：

```
nav: 646.5ms   导航起点 → 本 bundle 开始执行（含取回 + 解析）
ms:    4.3ms   本 bundle 开始执行 → 适配层装配完（17 段 CSS + 16 个 effect）
```

`nav` 那 646ms **主要不是我们的**：bundle 挂在「application」批次，前面是 dsh 前端壳自己的
启动（React + 壳 bundle + 模块图）。我们这一段是「进程内取字节 + 一个 script 元素 + 解析」。

### 看过、量过、**不做**的两件

1. **给 bundle 加长缓存头**（`Cache-Control: max-age=1y, immutable`）——
   ① 拦截发生在 HTTP 缓存**之前**，加了也省不掉这次 `shouldInterceptRequest` 回调；
   ② 真风险是「忘记换 rev」：1.0.53 出过一次旧缓存事故，当时的表现是**重启前一直旧的**，
      换成 `immutable` 就变成**清数据前一直旧的**。收益接近 0、失败代价放大，不做。
   （只补了 `Content-Length`：长度已知，渲染进程不必按未知长度流式处理。）
2. **压缩/去掉注释**（123,235 B 里 **49% 是注释**：25 KB 行注释 + 35 KB 块注释）——
   本机 V8 单次编译基准（每变体 6 个独立进程、各只编译一次）：
   **原文件 1.57ms vs 剥掉注释 2.47ms** —— 方向还相反，属噪声。原因是**注释不进 AST**：
   扫描器直接跳过，函数体又是惰性编译的，字节数不在关键路径上。
   为了省字节去动构建链（源码与验证 harness 共用一份、CI 还 `node --check` 它）不值得。

### 顺带核过的正确性面

Activity 销毁时 `onDestroy` 会把三个 client 换成不持 Activity 的兜底实现，重建时换回 ——
**两条路径都必须能喂出 bundle**，否则「Activity 不在的那段时间页面重载 = 适配层静默消失」。
1.0.70 之后两条路径都走缓存字节，行为一致。

## 设计收口：头部与输入区（1.0.87 – 1.0.89，2026-09-25）

用户：「页面有很多问题，需要优化设计」。先把两处最贵的**量出来**（布局诊断，报子树 + 矩形）：

```
header          y=18  h=46   w=360   ← 头部一条占 46px
  headerLeading y=20  h=44   w=24    ← 宿主的前导道，空的
  titleRow      y=20  h=44   w=288   ← 288px 宽里**没有一行文字**，只有两个图标
  tabs          y=0   h=0    w=0     ← 已被我们隐藏 ✓
composerSeat    y=652 h=144  w=339   ← 输入区占 144px（屏幕 800 的 18%）
```

### 改了三处

| 问题 | 改法 | 省 |
|---|---|---|
| 头部 46px，288px 宽的标题道里**没有标题**、两个图标浮在中间 | ① `titleRow` 压到 30px；② **补回会话标题**（宿主新版把标题挪去了侧栏，见下）；③ 标题用 `margin-right:auto` 把右侧动作/工具道**推到右端** —— 原来「浮在中间」正是因为左边没东西、右边也没东西可推 | ~16px + 视觉 |
| 输入区 144px：卡片 + `dock` 行（上下文表独占一行 ~28px） | `dock` 行压薄、卡片内行距收紧、上下文表压成 16px 细条（**不删** —— 上下文压力有用） | ~20px |
| 抽屉没有入口（槽版按钮随 fiber 死、宿主在"还有窄条"状态下不挂自己的入口） | 补一颗**裸 DOM 目录按钮**，点它去点宿主侧栏自己那颗 `_toggle` | 功能恢复 |

补标题的判据：`titleRow` 的 `textContent` 为空才补（图标没有文字，所以不会误判）；宿主哪天
又渲染标题，这段自动让位。**真机验证**：目录按钮已确认能开抽屉（点开后底色变 27,27,28、
抽屉内容完整）。头部与输入区的视觉结果**待验证** —— 装机后设备一直锁屏。

## ⚠️ 0.1.7-rc.2 会**拆掉注入的插件**（1.0.80 – 1.0.86，2026-09-25）

上面那节记的是「钩子改名」。真正严重的问题在换到新宿主**之后**才显形：手机上**整套适配
一条都不生效** —— 左侧栏常驻、页签露着、统计行还在底部，和没装插件一样。

### 怎么定位的（每一发都靠自报，不靠猜）

插件本身跑完了（`plugin-init` 出声、包大小与文件字节一致），所以嫌疑落在「样式为什么没生效」。
分四步钉死：

| 步骤 | 读数 | 结论 |
|---|---|---|
| ① 媒体查询 | `mq:1 mqW:1 mqP:1 innerW:384` | **不是**媒体查询不成立（我一开始猜的是这个，被读数否掉） |
| ② 样式表在不在 | `styleTag:false rules:-1` | 表**不在 DOM 里** |
| ③ 是没跑还是被拆 | `applies:1 styleAdds:1 styleRemoves:1` | 加了又被拆 —— 是 **fiber 被销毁** |
| ④ effect 还活着吗 | `turnTicks:0`（1 秒轮询一次没跑） | 所有 `ctx.effect` 全灭 |

对照证据：apply 里那些**裸 `setTimeout`** 照跑（t2/t12 两发都到）—— 说明不是页面冻住，是
**插件 fiber 被拆**。

### 机制（读宿主源码得到）

新版 `dsh-client-modules` 里有一对函数：

```js
/** Release a runtime before clearing its entry fiber so Loader refresh can import new code. */
async function tearDownEntryFiber(entry) { … }
/** Remove styles after their plugin's effect cleanup has settled. */
function removeOwnedStyles(id) { … }   // 凡 dataset.plugin === 该包 id 的 style 一律 remove()
```

加载器会按**服务端下发的清单**reconcile 一次 entry 图。我们的 entry 是 App 从
`__DSH_BOOT__` 塞进去的，**不在服务端清单里** —— 于是被当成「不该存在的东西」拆掉：
fiber 销毁（effect 全灭）+ 按包名删样式。旧版加载器不做这次 reconcile，所以以前一直没暴露。

### 修法：关键的三件事搬出 `ctx.effect`

`ctx` 没了就再也拿不回来，所以凡是「拆了也要活着」的部分都改成裸实现：

| 部件 | 旧 | 新 |
|---|---|---|
| 样式表 | `ctx.effect` 里 append | **看门狗**：每 2 秒查一次，不在就自己造一张（样式文本由看门狗持有） |
| `html.dsh-handheld-mobile` + `[data-handheld="frame"]` + 侧栏内联宽度 | effect 里打标 | 同一只看门狗一并补（实测拆后两个标记都会被撤掉） |
| 轮次监视（通知 + pageBusy 的页面侧唯一来源） | `ctx.effect` | 裸 IIFE + 全局旗标防重复安装 |
| 目录按钮 | `slots.register` 进会话头槽 | **裸 DOM 按钮**，点它去点宿主侧栏自己那颗 `_toggle`（那条路径不需要 ctx） |

**不能省的教训**（两个都是我自己的 bug，各让一轮白跑）：
1. 看门狗第一版把「拿到样式表」的钩子定义在 effect **之后** —— effect 先跑，看门狗手里是空的；
2. 第二版在「表还活着」时**提前 return** —— 表一活，补标与补按钮那两段永远不执行。

真机结果（设备 2，rev 1.0.86）：

```
t2  → t12:  aliveCss 1 ✓  reframes 1 ✓  frameTagged 1 ✓  turnTicks 2 → 12 ✓
App 侧:     页面报告：一轮生成开始（pageBusy=true）✓
视觉:       左侧常驻栏消失、顶部统计带就位、页签已隐藏
```

> ⚠️ 仍待验证：裸目录按钮能否真的把抽屉点开（装机时设备又进了锁屏）。
> 另外 `uiToggle: 0` —— 槽版注入的浮层/FAB 随 fiber 一起没了，目前只有目录按钮补了裸版本。

## 适配 dsh 0.1.7-rc.2（1.0.79，2026-09-25）

用户升级了宿主：**0.1.5-rc.2 → 0.1.7-rc.2**。契约检查（`node scripts/check-mobile-hooks.mjs`，
不带参数那档会去已安装的 dsh 产物里逐个断言）当场抓到一条**静默失效**：

```
✗ _turnStatus   **全量安装里也找不到** —— 这条适配规则已经空转
```

### 唯一的硬破坏：回合指示器被重做

旧版是 `div[class*="_turnStatus"][role=status][aria-live=polite]`，**回合结束就被 React 卸载** ——
所以轮次监视的判据是「`isConnected` 变 false = 结束」。新版换成了**每个回合一个「回合过程」节点**：

```
<button data-turn-process="<回合号>" data-turn-process-messages/-tool-calls/-subagents>
  <span>深度求索中，用时{duration}        ← 运行中每秒跳一次（LIVE_RUN_CLOCK_INTERVAL_MS）
        已完成工作 / 用时 {duration} / 处理失败   ← 结束后静态，**节点不卸载**
</button>
```

两处判据因此都换了：

| | 旧 | 新 |
|---|---|---|
| 选择器 | 哈希类名 `_turnStatus` | **语义钩子 `[data-turn-process]`**（一升级就变的类名不能再依赖） |
| 「在跑」 | `isConnected`（卸载即结束） | **标签在跳秒**：运行中标签每秒更新用时，**思考与工具调用期间也照跳**，且不依赖文案语种 |
| 挑哪个节点 | 优先可见的 | 优先可见的，其次**回合号最大**的（旧回合节点会留着当"已完成工作"摘要） |

代价与补偿：结束最多晚 3 秒才认出来（旧版卸载即报）；跳秒是 `characterData` 变更、`childList`
观察者收不到，所以另加一条 **1 秒轮询**驱动它（每次只读一个节点的 `textContent`，不碰布局；
空手时最多 5 秒重挑一次，避免每秒全树扫）。App 那边还有 Host 事件流这条权威路径对账
（见 DshApp 的两路信号），所以这点延迟可接受。

### 新功能审计（能静态定性的部分）

| 新版新东西 | 手机上的问题 | 结论 |
|---|---|---|
| `_inspectButton` / `_inspect`（工具卡 / skill 卡 / Cordis 卡 / deliverables 的「查看」） | **默认 `opacity:0`，靠 `.card:hover` 才显形** → 手机上"看不见就点不到" | **插件早已覆盖**（1.0.43 那条显形规则里就点了名）✓ |
| `TurnUsage` 等新统计触发器（28px） | 触控目标偏小 | 命中区扩展 `[class*="_trigger"]` 已覆盖 ✓ |
| 设置面板（账户/通用/插件/模型/预设，720–2200px） | 写死宽度 | 都带 `max-width: calc(100vw - 48px)` ✓；设置**对话框**那条 `:has(> nav)` 规则在新版结构里仍命中（`nav` 仍是直接子元素）✓ |
| 右栏新控件（`data-sidebar-right-toggle/expand/guide/unavailable`） | 触控/溢出 | `.entry/.entryCell` 是 `width:380px; max-width:100%` + `min-height:56px` ✓ |
| 回合过程 / 步骤过程行 | 触控/溢出 | 33px 整行按钮、标签 `text-overflow:ellipsis`、chevron 14px ✓ |
| 槽与服务（`shell.overlay`、`conversation.session.header.actions`、`layout.toggleSidebar`、`sessions.refresh`、`connection.reconnect`） | 改名就静默失效（钩子检查覆盖不到） | 逐个核对，**都在** ✓；引导图 entry 的 `id/url/rev` 校验也没变 ✓ |
| 结构假设（`<header>` + `_titleRow`、`_scrollBody` + `data-conversation-scroll`） | 结构搬家会让一堆 CSS 失效 | 都还在 ✓；新版另给了 `data-conversation-region` / `data-content-phase` 等语义钩子 |

契约同步：`classHooks` 删掉 `_turnStatus`，`dshHooks` 加入 `data-turn-process`
（**11 个 dsh 钩子 + 45 个类名后缀全部存在** ✓）。

> ⚠️ **真机验证未完成**：装机时两台设备都在锁屏（`mDreamingLockscreen=true`），
> 页面没渲染、体检与轮次自报都是空的。解锁后要核三件事：
> ① 轮次监视在新 DOM 上真的报出 `turn-start` / `turn-done`（跳秒判据）；
> ② 新版 UI（回合过程行、新统计、各面板）在手机上的实况；
> ③ 插件那套一次性体检（`tap-diag` 等）在新版上的读数。

## 统计行从输入框下方搬到页面最顶部（1.0.77 – 1.0.78，2026-09-25）

用户：「将会话页面最底部的统计信息移到最顶部」。那行（`26 轮 1017 步 · 143 tok/s` /
`289M tok · 缓存命中 97%`）本来挂在宿主 composer dock 槽里 —— 也就是**输入卡片下方**。

**做法：不搬 DOM，只改定位。** 那条行住在宿主的 React 树里，动 DOM 会被重渲染打回；
`position: fixed` 把它视觉上挪走，语义上仍归 composer：

```css
[data-composer-stats] {
  position: fixed; top: 0; left: 0; right: 0; z-index: 8;
  height: 16px; line-height: 14px; padding: 1px 8px 0; overflow: hidden;
  background: var(--dsw-alias-bg-base);
}
html[data-dsh-cover] [class*="_frame"]:has([data-phase]) { padding-top: 18px }
```

四处约束，每条都有理由：

| 约束 | 为什么 |
|---|---|
| `fixed` 可行 | 祖先链上**没有** `transform` / `will-change` / `contain`（宿主与插件两侧都核对过）。插件里那句「刻意用 left 而不是 transform」的注释说的正是这个坑 —— 有 transform 的话 `fixed` 会相对它定位、跟着内容滚 |
| `z-index: 8` | 压住正文与会话头（6），低于 FAB（21）与抽屉/右栏（40）—— 抽屉或面板打开时它正常被盖住 |
| 左右两个胶囊分列两端 | 屏幕顶部正中是**挖孔**（设备 1 为 128、设备 2 为 88 设备像素），中间那点空档正好避开它；状态栏与手势条由 App 隐藏（`hide(statusBars() or navigationBars())`），所以这条带子上没有系统 UI |
| **高度写死 16px** | 1.0.77 先按内容定高，那意味着带子可能比外壳让出的 17px 高、把下面的会话头压住（实测那行 ink 有 ~27 CSS）。改成 `height + line-height + overflow hidden` 的确定几何后，18 = 16 + 2 的让位在任何内容下都成立。胶囊没有背景色，裁掉的内边距看不出来 |

**代价**：会话页顶部多 6px（外壳 12 → 18），换来的是常驻可见的统计行与更干净的底部。

**真机验证**（设备 2 / 1080×2400 / 360 CSS 宽，rev 1.0.78，解锁后逐项核对）：

| 检查项 | 结果 |
|---|---|
| 带子位置与高度 | 内容在 y 6..45 设备像素 = **CSS 0..16** ✓（写死的 16px 生效） |
| 两个胶囊分列两端 | 内容 x 13..347 CSS（两端有、中间空）✓ 挖孔正中不被压 |
| 标题行完整可见 | 标题行在 CSS 27..42，与带子（0..16）**无重叠** ✓ |
| 抽屉打开时 | 顶部像素变成抽屉底色 (27,27,28) → **被盖住** ✓ |
| 右栏打开时 | 顶部带 0..16 CSS 内与「无面板」状态**差异 10296/17280 像素** → **被盖住** ✓ |

底部同时变干净了（输入框下面不再有那一行）。若带子显得太挤，改 `height`/`line-height`
与外壳 `padding-top` 这两个数即可。

## 注入的代码会不会影响流式响应？（1.0.74 – 1.0.76 取证，2026-09-25）

用户问「流式响应不及时，是不是之前改了什么」。分三层量，结论是**注入层不是原因**。

### ① 流式期间的帧统计（设备 2，30s 实测）

| 指标 | 数值 |
|---|---|
| 帧数 / janky | 3540 帧 / **0.34%** |
| 99th / missed vsync | **8ms** / **0** |
| 帧计数每 1.5s 采样（26s） | +181 ~ +200，**无一次停顿** |

渲染侧没有可归因的劣化。

### ② 流式期间注入的代码**跑不跑**（这是关键）

流式期间真正可能反复跑的只有三个回调。给它们各自装了计时（每 10 秒一报，包装只多两次
`performance.now()`），并在同一个窗口里放了一个**只数不做事**的 `characterData` 观察者作对照：

```
{"what":"js-cost","ms":0,"mark":0,"markN":0,"turn":0,"turnN":0,"ask":0,"askN":0,
 "vis":"visible","cdN":10}          ← 连续 8 个 10 秒窗口都是这样
```

`cdN=10` = 每秒 1 次 `characterData` 变更（会话页那个「深度求索中… N 秒」计时器），
**说明页面在变**；而 `markN/turnN/askN` 全是 0 —— **我们那三个观察者一次都没被触发**。

原因很朴素：它们订阅的是 `childList: true`，而**流式追加文字改的是文本节点内容
（`characterData`）**，`childList` 根本收不到。所以流式期间注入层的 JS 开销**是 0**，
不是"很小"。

### ③ 那它什么时候跑、跑一次多贵

制造一次结构性变更（开合抽屉）后的读数：

```
{"ms":1.4,"mark":0.1,"markN":3,"turn":0,"turnN":3,"ask":1.3,"askN":3}
```

即 10 秒 1.4ms，其中 `ask` 占 1.3ms / 3 次 ≈ **每次 0.43ms**（那是它"没有待输入节点"时走的
全树 `querySelector`）。也就是说：**它只在结构性变更时按次付费，而流式不在其列**。

> **诚实的边界**：这套取证证明的是**注入层**。宿主的渲染/流式代码、隧道与 WebSocket
> 数据通道都不在这份代码里 —— 如果"不及时"指的是"文字比电脑上晚到"，那要量的是那条通道，
> 不是这里。另外，「流式文字本身是否产生 `childList`」是由计时器的 `characterData` 读数
> 加上机制推出来的（我没抓到一次纯文字流式的窗口来直接看 `cdN` 暴涨），这一条属于推断而非直测。

### 顺带发现并修掉的一个真坑：BACK 与键盘

真机日志（设备 2 / Android 13）：

```
00:14:06.406  DshHandheld: BACK: 无历史 → 回连接屏
00:14:06.425  DshHandheld: screen: WEB → CONNECT
```

**键盘弹起时按返回，App 会把你切回连接屏** —— 而流式照旧在后台的 WebView 里跑
（帧计数实测仍在 +480/4s），屏幕上却什么都看不到，看起来就是「流式不动了」。
而且它**时灵时不灵**：同样「键盘弹起 + BACK」，有时是输入法自己吃掉的。

修法：`onBackPressed` 最前面加一级 —— 键盘可见（由 root 的 insets 监听维护）就只收键盘。
键盘不可见时返回阶梯一个字没改。

## 会话页再省两处：页签与输入框宽度（1.0.73，2026-09-24）

用户两条要求：「轨迹无法点击，可以完全隐藏对话和轨迹按钮节约空间」＋「输入框没有利用左右空间」。

### ① 隐藏「对话 / 轨迹」页签

`[class*="_tabs"]` 整条 `display: none` —— 省下页签 25px + 上边距 4px ≈ **29px** 竖向空间，
会话头从「标题行 + 页签行」变成只有标题行（无障碍树里原来那条 `[60,138][996,213]` 的页签容器
直接消失，标题行下面就是消息）。

> ⚠️ **这不是在修 bug。** 真机复验（设备 2 / Android 13 / rev 1.0.72）：点「轨迹」整屏
> **62.75% 的像素变化**、视图确实切到轨迹，无障碍树里它也是 `clickable=true`。
> 所以「轨迹无法点击」在设备 2 上**复现不了** —— 按用户要求省空间，代价是**「轨迹」视图
> 在本 App 里没有入口了**（宿主只在会话头提供这一个切换口）。想恢复：删掉 `display` 那一行。

### ② 输入框吃满左右空间

真机实测（设备 2，360 CSS 宽）：输入卡片边界 **CSS 28.0..331.7（宽 303.7）**，而正文文字的
ink 是 24..335.7 —— 卡片比正文还窄 4px，两侧各空 28px。那 28px 是**两层叠出来的**：

```
12px = 我们第 11 节给滚动体 [class*="_scroll"] 的左右内边距
16px = 宿主 .uV2eYG_root 的 padding: 0 var(--dsh-composer-side-clearance) 8px（变量默认 16px）
```

顺带排掉一个**看似可能的元凶**：`--dsh-composer-card-max-width = clamp(680px,…,920px) + 32px`，
在 360 宽的屏上根本不生效 —— 所以卡住宽度的就是上面这两层内边距，不是 max-width。

改法：把变量挂在 **`data-composer-seat`**（宿主自己写的稳定属性，不是哈希类名）上，归零。
它只覆盖输入框那一棵子树 —— 正文的 12px、各种卡片自己的 `calc(clearance + 16px)` 都不受影响。

| | 改前 | 改后 |
|---|---|---|
| 输入卡片左右边界 | CSS 28.0..331.7 | **12.0..347.7** |
| 宽度 | 303.7 | **335.7（+32）** |

卡片与正文**容器**同宽，卡片内占位文字起点 24 = 正文 ink 起点 24 ✓。键盘弹起时同样成立
（卡片底 CSS 428.7 < 键盘顶 458），标题仍在顶部（`mInputShown=true`）。

## 版本与缓存

`MainActivity.MOBILE_PLUGIN_REV` 是 WebView 侧的缓存键：**内容变了必须换 rev**，否则可能
命中旧缓存。CI 断言 App 常量与 bundle 内的 `id` 一致（`check-mobile-hooks.mjs` 的静态
不变量；`mobile-contract` 里还有一步 `node --check` —— CSS 写在模板字面量里，注释里一个
反引号就能把模板提前结束）。

当前为 `dsh-handheld-mobile-1.0.89`。

> 1.0.89 = 设计收口（补会话标题、图标靠右、上下文表压细条）；1.0.87/1.0.88 = 布局诊断与头部/输入区压缩；1.0.86 = 看门狗修掉提前 return（补标/补目录按钮）；1.0.85 = 裸 DOM 目录按钮；1.0.84 = 关键部件搬出 ctx.effect（被拆也活着）；1.0.82 = 样式自愈；1.0.80/1.0.81 = 环境诊断；1.0.79 = 适配 dsh 0.1.7-rc.2（回合指示器重做，轮次监视改用 data-turn-process + 跳秒判据）；1.0.78 = 统计行搬到会话页最顶部（高度写死 16px，头部让位 18px）；1.0.77 = 统计行 fixed 到最顶部；1.0.76 = 流式期间 JS 开销归因（结论：注入层 0 次触发）；1.0.73 = 隐藏会话页的「对话/轨迹」页签（省 ~29px）+ 输入框吃满左右空间（卡片 303.7 → 335.7）；1.0.72 = 侧边栏顶部空间与会话页齐平（结构定位真正的侧栏根）+ 一次性 topspace-diag；1.0.71 = 先只对齐外层（实测不够，见「侧边栏的顶部空间」一节）；1.0.70 = 注入热路径改走缓存字节（每次页面加载省一次 123KB 读盘）＋ `Content-Length`＋ `isPluginBundleUrl` 早退＋插件自报注入时序；1.0.69 = 面板遮挡轮转采样（打开时每次只强制一次布局，实测自报条数 6→3、单次 5.3–7.9→4.2–4.8ms）；1.0.68 = 只改注释：撤回 1.0.67 里那句错误的「顺带修掉一个语义错」（真机 right-probe 取证，见性能一节），行为与 1.0.67 完全一致；1.0.67 = 面板遮挡的开合判据前置到读几何之前（收起时零强制布局）＋面板元素缓存；1.0.66 = 9 条文档级
> `:has()` 换成 html 类、侧栏行内操作重扫降到 ≤2 次/秒；1.0.65 = 面板采样的 2 秒节流真的生效
> （原先节流排在几何读之后，等于没省）；1.0.64 = `--dsh-handheld-vh` 只在高度变化时写；
> 1.0.63 = 左右侧边栏不再退避状态栏，与会话页一样全屏；1.0.62 = 键盘弹起时顶栏钉住 + 用视觉视口
> 高度绑定页面高度（1.0.60 只 sticky 会把输入框挤出屏幕、1.0.61 用 `scrollIntoView` 会把顶栏滚走，
> 两版都被真机否掉）；1.0.59 = 会话行的归档/分支/重命名修好闭包抓错会话；

> 1.0.43 = Web UI 逻辑复健（会话列表空则 `ctx.sessions.refresh()`；回前台仍空则 `ctx.connection.reconnect()`）
> ＋ 三类「hover 才亮却仍吃点击」的隐形控件统一显形；

> 1.0.42 = 把行尾 `⋯` 的菜单项摊平成按钮（插件注入 + 走宿主自己的菜单，不绕过它的确认框）；最近的几档：1.0.41 = 用 JS 按几何找出宿主侧栏根节点并改写它的内联 width（纯 CSS 那条压不到，
因为槽运行时又包了一层）；1.0.40 = 全屏抽屉把空间用起来（行高/字号/图标按手指档抬一档）；1.0.39 = 左侧边栏展开即全屏（100vw）＋ 里面所有
hover-only 的按钮（行 ⋯、工作区 +、展开箭头）都恒显示，连我们自己藏的那颗「添加工作区」也放回来了；
1.0.38 = 会话行的 `⋯` 也不被 hover 藏起来
（`_sessionRow` 的 `_rowActions` 恒显示）；1.0.37 = 工作区行上的 `⋯`/`+` 不再被 hover 藏起来
（`_projectRow` 的 `_rowActions` 恒显示；1.0.36 是一次被撤回的尝试 —— 把头部提到抽屉之上，
用户澄清说的其实是这一处，所以那版已整条退回）；1.0.35 = 头部左右按钮的竖直对齐（目录按钮的
`top` 改成跟 header 的 `padding-top` 共用同一个变量）；1.0.34 = 压掉宿主的滚动条槽（左右各 12px 对称）+ `side-diag`；1.0.33 = 压掉宿主外壳写死的 35px 安全区内边距；
1.0.31/1.0.32 = `layout-diag`（祖先链 + `env()` 探针，就是把上面那条揪出来的工具）；
1.0.30 = 头部左右按钮对称（8 → 12px）；1.0.29 = 右端按钮贴边回归的修复；1.0.28 = 统计行被省略号
吃掉 + `stats-diag`；1.0.25 = 统计行一行到底 + CI 语法检查；1.0.19 = 任务完成通知的判据改取可见节点。

> 一遍遍地踩同一个坑：**内容改了就必须换 rev**，否则手机的 WebView 缓存会把旧的那份喂回来
> （1.0.17、以及 `viewport-fit=cover` 那次都漏换过）。
